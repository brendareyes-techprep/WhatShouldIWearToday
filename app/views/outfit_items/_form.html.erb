<%= form_with(model: outfit_item) do |form| %>
  <% if outfit_item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(outfit_item.errors.count, "error") %> prohibited this outfit_item from being saved:</h2>

      <ul>
        <% outfit_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
